<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Delete Fragrance</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">

    <% if (user && user.role === 'admin') { %>
      <h2>Delete Fragrance</h2>

      <div class="card mb-3">
        <div class="row g-0">
          <div class="col-md-4">
            <img src="/images/<%= fragrance.image %>" class="img-fluid rounded-start" alt="<%= fragrance.name %>">
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title"><%= fragrance.name %></h5>
              <p class="card-text">Price: $<%= fragrance.price.toFixed(2) %></p>
              <p class="card-text">Quantity: <%= fragrance.quantity %></p>
              <p class="card-text text-danger fw-bold">Are you sure you want to delete this fragrance?</p>

              <form action="/deleteFragrance/<%= fragrance.productId %>" method="POST">
                <button type="submit" class="btn btn-danger">Yes, Delete</button>
                <a href="/inventory" class="btn btn-secondary">Cancel</a>
              </form>
            </div>
          </div>
        </div>
      </div>

    <% } else { %>
      <div class="alert alert-danger">Access denied. Admins only.</div>
    <% } %>

  </div>
</body>
</html>
